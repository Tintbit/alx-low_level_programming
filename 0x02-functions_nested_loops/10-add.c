#include "main.h"
/**
 * add- adding to numbers and return the summition
 * @x: int number
 * @y: int number which will be added to x
 * Return: x + y
 */

int add(int  x, int y)
{
	return (x + y);
}
