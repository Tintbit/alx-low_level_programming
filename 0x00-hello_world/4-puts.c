#include <stdio.h>

/**
 * main - returns int value, takes no arg!
 * description: output msg to stdout stream
 *
 * Return: Always (0)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
