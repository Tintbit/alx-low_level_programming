#include <stdio.h>

/**
 * main - testing file!
 * description: used to Ensure that everthing yr doing
 * at this moment(piece by piece, bit by bit) is working fine till this point!
 *
 * Return: Always (0)!
 */
int main(void)
{
	printf("it's what it's");
return (0);
}
