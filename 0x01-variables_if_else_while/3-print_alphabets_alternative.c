#include <stdio.h>

/**
 * main - entry point.
 *
 * Description:
 * Return: Always (0).
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	
		putchar(n);


	for (c = 'A'; c <= 'Z'; c++)
	
		putchar(n);
	

	putchar('\n');

	return (0);
}
